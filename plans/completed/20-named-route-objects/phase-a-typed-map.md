# 20.A — Rule, typed route map, `AppRoute`, guard (report mode)

**Status: done** (2026-09-26). Build green, `vue-tsc` catches a bad route name (verified),
`node scripts/check-routes.js` runs (baseline was ~485, not ~400 — counting differs slightly from
the plan's estimate but same order of magnitude). e2e suite not run this session.

Goal: route names and params are checked by `vue-tsc` before any call site moves to objects, and
the rule is written down so new code follows it from today. After this phase the app behaves
exactly as before. Only types, a generated file, a script and docs change.

## Tasks

### A1 — Write the rule into CLAUDE.md (first, so new code follows it during the migration)
- [ ] Add rule **25** under `## UI rules` → `### Navigation` in `CLAUDE.md`:
  > 25. **Every navigation target is a named route object**: `{ name, params?, query?, hash? }`.
  >     Never a path string (`'/invoices'`, `` `/invoices/${id}` ``) and never `{ path: … }`. This covers
  >     `router.push/replace`, `RouterLink`/`AppButton`/`KpiCard` `:to`, `PageHeader :back`, sidebar
  >     and quick-action items, settings tabs, palette commands, service link fields (`actionTo`,
  >     `sourceLink`, `refLink`, `link`) and route-record `redirect`s. Type any prop or field that
  >     holds a destination as `AppRoute` (`core/types/route.ts`). Route names and params are
  >     type-checked by the generated `src/router/route-map.gen.d.ts`. Read params with
  >     `useRoute('<route-name>')`. Same-page query/hash updates may leave out `name`
  >     (`router.replace({ query: { ...route.query, x } })`). `scripts/check-routes.js` enforces this
  >     (it joins `bun run check` in plan 20.D). The only escape is `/* route-ok: <reason> */`, for a URL
  >     round-trip such as the login `?redirect=` fullPath.
- [ ] Add to `## Don't`: "Don't navigate with path strings or `{ path }`. Use `{ name, params, query }` (rule 25)."
- [ ] In `### Keeping the memory current`, add: "`bun run memory` also regenerates
  `src/router/route-map.gen.d.ts` (typed route names). Adding, renaming or removing a route
  without re-running it breaks `vue-tsc`."

### A2 — Generate the typed route map from the route records
- [ ] `scripts/memory/config.ts`: add `routeMapOutput: 'src/router/route-map.gen.d.ts'` to the config
  type and value. Add landmark `['Typed route names (generated)', 'src/router/route-map.gen.d.ts']`.
- [ ] New `scripts/memory/render/routeMap.ts` → `renderRouteMap(routes: RouteInfo[]): string`:
  - Only records with a `name`. Sort by name so the output is deterministic.
  - Params come from the path: `:id` → raw `string | number`, normalized `string`. `?` → optional.
    `*` / `+` (and `/:pathMatch(.*)*`) → arrays (`(string | number)[]` raw, `string[]` normalized;
    optional for `*`).
  - **Throw** on a duplicate name. vue-router would silently keep the last one.
  - Output shape:
    ```ts
    // GENERATED by `bun run memory` from src/modules/*/routes — do not edit.
    import type { RouteRecordInfo } from 'vue-router';
    export interface RouteNamedMap {
      'invoice': RouteRecordInfo<'invoice', '/invoices/:id', { id: string | number }, { id: string }>;
      'home': RouteRecordInfo<'home', '/', Record<never, never>, Record<never, never>>;
      // …
    }
    declare module 'vue-router' {
      interface TypesConfig { RouteNamedMap: RouteNamedMap }
    }
    ```
- [ ] `scripts/memory/run.ts`: write the map next to `AGENT_MEMORY.md` (only when changed), and make
  `--check` fail when either output is stale. Name the stale file in the error.
- [ ] Check the parser against the real router. `parseRoutes` must find the dev-only records in
  `core/routes/index.ts` (`dev-ui`, `dev-diagnostics`) and the 18.B routes (`settings-audit-log`,
  `settings-about`). `AGENT_MEMORY.md` lists 122 route records on 2026-09-26. Gate: map entry count = `router.getRoutes().filter(r => r.name).length`
  in a dev build.

### A3 — `AppRoute` type
- [ ] New `src/modules/core/types/route.ts`:
  ```ts
  import type { RouteLocationAsRelative } from 'vue-router';
  import type { RouteNamedMap } from '@/router/route-map.gen';
  export type RouteName = keyof RouteNamedMap;
  /** A navigation target: a named route object. `name` is required; params are checked per route. */
  export type AppRoute<N extends RouteName = RouteName> = { [K in N]: RouteLocationAsRelative<K> & { name: K } }[N];
  ```
  The mapped-then-indexed form keeps `params` tied to its own `name` (a union, not a merge).

### A4 — Typed `useRoute` where params are read (keeps `vue-tsc` green once the map is on)
Switch `useRoute()` → `useRoute('<name>')` (or `useRoute<'a' | 'b'>()` for pages that several routes
mount; read optional ids with `'id' in route.params ? route.params.id : undefined`):
- [ ] accounting: `JournalDetailPage` (`journal-entry`)
- [ ] expenses: `ExpenseDetailPage` (`expense-detail`)
- [ ] invoices: `InvoiceDetailPage` (`invoice`), `InvoicePrintPage` (`invoice-print`), `QuotationDetailPage` (`quotation`), `RefundPage` (`invoice-refund`), `ShiftReportPage` (`pos-shift-report`)
- [ ] parties: `PartyDetailPage` (`customer` | `supplier`), `PartyFormPage` (`customer-new` | `customer-edit` | `supplier-new` | `supplier-edit`)
- [ ] payments: `PaymentDetailPage` (`payment-detail`)
- [ ] products: `ProductDetailPage` (`product`), `ProductFormPage` (`product-new` | `product-edit`), `StockAdjustmentDetailPage` (`adjustment`), `StockCountDetailPage` (`count`)
- [ ] purchases: `PurchaseDetailPage` (`purchase`), `PurchaseFormPage` (`purchase-new` | `purchase-edit`), `PurchasePrintPage` (`purchase-print`), `PurchaseReceivePage` (`purchase-receive`), `PurchaseReturnPage` (`purchase-return`)
- [ ] templates: `TemplateDesignerPage` (`settings-template-designer`)
- [ ] users: `UserEditorPage` (`user-editor`)
- [ ] vouchers: `VoucherDetailPage` (`voucher-detail`), `VoucherPrintPage` (`voucher-print`)
- [ ] `invoices/commands.ts`, `purchases/commands.ts`, `vouchers/commands.ts`: they read
  `router.currentRoute.value.params.id` inside `run`. Narrow first
  (`const r = router.currentRoute.value; if (r.name !== 'invoice') return;`). Their `when` already
  checks the route name.
- [ ] Fix any other `route.name === '…'` comparison that `vue-tsc` now flags. A flag there means a
  typo that was always false.

### A5 — `scripts/check-routes.js` (report mode; joins `bun run check` in 20.D)
Same style as `scripts/check-rtl.js`: Node, `globSync`, file/line findings, exit 1 when any exist.
- [ ] Scan `src/modules/**/*.{vue,ts}` and `src/router/**/*.ts` (not `src/mocks`, not `*.gen.d.ts`).
- [ ] Rule 1, **route-path literals** (catches every syntax: ternaries, helpers, config arrays,
  template attributes). Flag any `'…'`, `"…"` or `` `…` `` literal, or a static attribute value, whose text
  starts with `/<segment>` where `<segment>` is the first path segment of any route record. Build
  the list from `src/modules/*/routes` so it grows by itself (today: `accounting analytics approvals
  catalog customers dev expenses forbidden inventory invoices login payments pos print products
  purchases reports sales settings setup suppliers users vouchers welcome`). Example it catches that a
  call-site pattern would miss: `return d.existingId.startsWith('sup') ? `/suppliers/${…}` : …`
  (`PartyFormPage.vue:197`).
- [ ] Rule 2, **home and path objects**: `to="/"`, `:to="'/'"`, `push('/')`/`replace('/')`,
  `return '/'`, and `{ path:` used as a target (push/replace argument, `:to`, `:back`, `actionTo`,
  `redirect`). A bare `'/'` is only flagged in these spots, so `split('/')` and `join('/')` are fine.
- [ ] Skip `path: '…'` of route records (`src/modules/*/routes/*.ts`, `src/modules/*/routes.ts`). In those
  files only `redirect:` values are checked.
- [ ] A line with `/* route-ok: <reason> */` is skipped (empty reason = still a finding).
- [ ] Output groups findings per file and ends with a total. Record the baseline total in this
  file's status note (expected about 400).

### A6 — Index + memory
- [ ] `bun run memory` (new generated file, new script = structural). Commit `AGENT_MEMORY.md` and
  `src/router/route-map.gen.d.ts` together.

## Gate
- [ ] `bun run build` green with the typed map on (A4 done).
- [ ] Map entry count = named routes in `router.getRoutes()` (dev build).
- [ ] Making a typo in one route name on purpose (e.g. `useRoute('invoce')`) fails `vue-tsc`. Revert it.
- [ ] `node scripts/check-routes.js` runs and prints the baseline. It is **not** in `bun run check` yet.
- [ ] `bun run check`, `bun run verify:mocks`, `bun run memory:check`, full e2e suite, no console errors.
